import { Layout } from './'

const DashboardLayout = ({children}) => {  
  return (
    <Layout>
      <p>Dashboard Layout</p>
      {children}      
    </Layout>
  )
}

export default DashboardLayout
